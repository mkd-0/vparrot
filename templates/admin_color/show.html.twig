{% extends 'base.html.twig' %}

{% block title %}Couleurs{% endblock %}

{% block body %}
    <h1>Couleurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ color.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ color.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_color_index') }}">Retour</a>

    <a href="{{ path('app_admin_color_edit', {'id': color.id}) }}">Modifier</a>

    {{ include('admin_color/_delete_form.html.twig') }}
{% endblock %}
