<form method="post" action="{{ path('app_admin_car_delete', {'id': car.id}) }}" onsubmit="return confirm('Êtes vous sûr?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car.id) }}">
    <button class="btn">Supprimer</button>
</form>
