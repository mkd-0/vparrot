<form method="post" action="{{ path('app_admin_customer_delete', {'id': customer.id}) }}" onsubmit="return confirm('
Êtes vous sûr?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customer.id) }}">
    <button class="btn">Supprimer</button>
</form>
