<nav class="navbar navbar-expand-lg custom-navbar">
    <div class="container-fluid">
      <img src="{{ asset('asset/logo_vparrot.png') }}" alt="Garage VParrot" class="img-fluid" style="width:8%">
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav nav-link-custom">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page"  style="color: white;" href="{{path('homeIndex')}}">Accueil</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" style="color: white;" href="{{path('occasions_index')}}">Nos occasions</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" style="color: white;" href="{{path('app_contact')}}">Contact</a>
          </li>
         {% if app.user %}
            <li class="nav-item">
                <a class="nav-link btn btn-secondary" style="color: white;" href="{{path('admin_index')}}">Configuration</a>
            </li>
        
       {%endif %}
        </ul>
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

       {% if app.user %}
            <li class="nav-item">
                <a class="nav-link btn btn-secondary" style="color: white;" href="{{path('app_logout')}}">Me déconnecter</a>
            </li>
        {% else %}
            <li class="nav-item">
                <a class="nav-link" style="color: white;" href="{{path('app_login')}}">Administration</a>
            </li>
       {%endif %}
            
            
        </ul>
      </div>
    </div>
  </nav>