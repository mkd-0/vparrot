{% extends 'base.html.twig' %}

{% block title %}Brand{% endblock %}

{% block body %}
    <h1>Brand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ brand.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_brand_index') }}">Revenir</a>

    <a href="{{ path('app_admin_brand_edit', {'id': brand.id}) }}">Modifier</a>

    {{ include('admin_brand/_delete_form.html.twig') }}
{% endblock %}
