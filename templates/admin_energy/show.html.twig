{% extends 'base.html.twig' %}

{% block title %}Energie{% endblock %}

{% block body %}
    <h1>Energie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ energy.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ energy.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_energy_index') }}">Revenir</a>

    <a href="{{ path('app_admin_energy_edit', {'id': energy.id}) }}">Modifier</a>

    {{ include('admin_energy/_delete_form.html.twig') }}
{% endblock %}
